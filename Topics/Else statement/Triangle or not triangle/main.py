alpha = float(input())
beta = float(input())
gamma = float(input())
sum_triangle = 180

print("The triangle is valid!" if alpha + beta + gamma == sum_triangle
      else "The triangle is not valid!")
